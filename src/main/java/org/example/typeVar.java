package org.example;

public class typeVar {
    // var blabla = "does noet work here";
    public void whatType() {
        var name = "x";
        var size = 7;
        // size = "cant change to string";
        var silly
                = 1;
       // var question;
       // question = "wrong, var has to be initialized on the same line;

        // int a, var b = 3
        // does not work since initializing on the same line has to be of the same type

        // var n = null;
        // not possible
    }

/*    public int addition(var a, var b) {
        return a + b
    }*/
    // var cannot be used as parameter variables
}
